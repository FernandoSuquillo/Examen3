apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-claims
spec:
  selector:
    matchLabels:
      app: mysql-claims
  template:
    metadata:
      labels:
        app: mysql-claims
    spec:
      containers:
      - name: mysql
        image: mysql:8.0
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: password
        - name: MYSQL_DATABASE
          value: claims_db
        ports:
        - containerPort: 3306
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-claims
spec:
  selector:
    app: mysql-claims
  ports:
    - port: 3306
      targetPort: 3306
